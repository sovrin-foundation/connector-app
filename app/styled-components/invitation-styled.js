import React from 'react'
import { Image } from 'react-native'
import styled from 'styled-components/native'

// TODO: Delete this once we move all invitation into components folder

export const ForwardArrow = styled.Image`
  margin-top: 30;
`
