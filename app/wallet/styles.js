import { StyleSheet } from 'react-native'
import { PADDING_HORIZONTAL } from '../common/styles/constant'

const styles = StyleSheet.create({
  tabContainer: {
    maxHeight: 60,
  },
  headerCloseIcon: {
    marginRight: PADDING_HORIZONTAL,
  },
  sovrinLogoWrapper: {
    paddingTop: 10,
  },
  balanceHeader: {
    paddingBottom: 15,
  },
})

export default styles
