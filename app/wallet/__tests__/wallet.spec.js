import React from 'react'
import renderer from 'react-test-renderer'
// import Wallet from '../wallet'

describe('<Wallet />', () => {
  xit('should render Wallet', () => {
    // const wrapper = renderer.create(<Wallet />).toJSON()
    // expect(wrapper).toMatchSnapshot()
  })
})
