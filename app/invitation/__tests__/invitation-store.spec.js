//@flow

import 'react-native'
import React from 'react'

import { getKeyPairFromSeed, randomSeed } from '../../services/keys'
import bs58 from 'bs58'
import { PENDING_CONNECTION_REQUEST_CODE } from '../../common/api-constants'

describe('Authentication request tests', () => {
  it('Authentication request, should update invitation store properly', () => {
    // TODO:PS: Add authentication-request tests
  })
})
