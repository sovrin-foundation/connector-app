import 'react-native';
import React from 'react';

// Note: test renderer must be required after react-native.
import renderer from 'react-test-renderer';

xit("App should work correctly", () => {
  // TODO: Add tests for Android
});
