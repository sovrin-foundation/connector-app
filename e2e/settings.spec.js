describe('Settings', () => {
  it('Choose profile photo', async () => {
    // enter Pin Code screen
    // user should redirect to dashboard screen
    // test footer tabs is preset
    // tap on settings
    // user should redirect to settings screen
    // User avatar, user name, pass-code and enable touch-id should be present on settings list
    // tap on user avatar
    // test camera should open
    // tap on home icon in footer
    // user should back to dashboard screen
  })

  it('Setup Pass-code', async () => {
    // tap on settings
    // tap on pass-code
    // pass-code screen should come up
    // tap on home icon in footer
    // user should back to dashboard screen
  })

  it('Enable Touch-Id', async () => {
    // tap on settings
    // tap on Enable Touch-Id
    // touchId enable screen should come up
  })
})
